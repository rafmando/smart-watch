import { Roboto, Chakra_Petch } from 'next/font/google';

export const robohobo = Roboto({ weight: ['900'], subsets: ['latin'] });
export const chakrapetchy = Chakra_Petch({ weight: ['700'], subsets: ['latin'] });
